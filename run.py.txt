from dotenv import load_dotenv
import uvicorn
import asyncio
from app.main import app

load_dotenv()

if __name__ == "__main__":
    print("🚀 War Trading System - Top Tier Mode Starting...")
    uvicorn.run("app.main:app", host="0.0.0.0", port=8000, reload=True)